import { StyleSheet } from 'react-native';

// Splash Screen Styling from Template, edited
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#2cd18a'
  },
  photo: {
    width: 150,
    height: 150
  }
});

export default styles;
